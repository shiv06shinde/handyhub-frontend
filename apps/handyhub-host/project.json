{
  "name": "handyhub-host",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "handyhub",
  "sourceRoot": "apps/handyhub-host/src",
  "tags": ["scope:handyhub-host"],
  "targets": {
    "build": {
      "executor": "@angular/build:application",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/handyhub-host",
        "browser": "apps/handyhub-host/src/main.ts",
        "polyfills": ["zone.js"],
        "tsConfig": "apps/handyhub-host/tsconfig.app.json",
        "assets": [
          {
            "glob": "**/*",
            "input": "apps/handyhub-host/public"
          }
        ],
        "styles": ["apps/handyhub-host/src/styles.css"],
        "server": "apps/handyhub-host/src/main.server.ts",
        "ssr": {
          "entry": "apps/handyhub-host/src/server.ts"
        },
        "outputMode": "server"
      },
      "configurations": {
        "production": {
          "budgets": [
            {
              "type": "initial",
              "maximumWarning": "500kb",
              "maximumError": "1mb"
            },
            {
              "type": "anyComponentStyle",
              "maximumWarning": "4kb",
              "maximumError": "8kb"
            }
          ],
          "outputHashing": "all"
        },
        "development": {
          "optimization": false,
          "extractLicenses": false,
          "sourceMap": true
        }
      },
      "defaultConfiguration": "production"
    },
    "serve": {
      "dependsOn": ["api:serve"],
      "continuous": true,
      "executor": "@angular/build:dev-server",
      "configurations": {
        "production": {
          "buildTarget": "handyhub-host:build:production"
        },
        "development": {
          "buildTarget": "handyhub-host:build:development"
        }
      },
      "defaultConfiguration": "development",
      "options": {
        "proxyConfig": "apps/handyhub-host/proxy.conf.json"
      }
    },
    "extract-i18n": {
      "executor": "@angular/build:extract-i18n",
      "options": {
        "buildTarget": "handyhub-host:build"
      }
    },
    "serve-static": {
      "continuous": true,
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "handyhub-host:build",
        "port": 4200,
        "staticFilePath": "dist/apps/handyhub-host/browser",
        "spa": true
      }
    }
  }
}
